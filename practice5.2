#include <stdio.h>

void main()
{
      int n1, n2, tmp;
      
      printf("정수 2개를 입력 하세요:");
      scanf_s("%d %d", &n1, &n2);
      if ( n2>n1 ){
            tmp = n1;
            n1 = n2;
            n2 = tmp;
      }
      printf(" n1 : %d, n2 : %d \n", n1, n2);
 }
